---
import type { CatalogEntries } from "../i18n/catalog-schema";
import type { LocaleDefinition, LocaleRegistry } from "../i18n/locale-registry";
import { buildPageMetadata, serializeStructuredData } from "../seo/page-metadata";
import "../styles/global.css";

interface Props {
  siteUrl: string;
  locale: LocaleDefinition;
  registry: LocaleRegistry;
  common: CatalogEntries;
  title: string;
  description: string;
  metadata?: ReturnType<typeof buildPageMetadata>;
  indexable?: boolean;
}

const { siteUrl, locale, registry, common, title, description, metadata: suppliedMetadata, indexable = true } = Astro.props;
const metadata = suppliedMetadata ?? buildPageMetadata({ siteUrl, locale, registry, title, description });
---

<!doctype html>
<html lang={locale.code} dir={locale.direction}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="robots" content={indexable ? "index, follow, max-image-preview:large" : "noindex, nofollow"} />
    {indexable ? <link rel="canonical" href={metadata.canonical} /> : null}
    {indexable ? metadata.alternates.map((alternate) => (
      <link rel="alternate" hreflang={alternate.hreflang} href={alternate.href} />
    )) : null}
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={metadata.canonical} />
    <meta property="og:locale" content={locale.code.replace("-", "_")} />
    <title>{title} | {common.brand}</title>
    {indexable ? <script
      type="application/ld+json"
      is:inline
      set:html={serializeStructuredData(metadata.structuredData)}
    /> : null}
  </head>
  <body>
    <a class="skip-link" href="#main">{common.skip_to_content}</a>
    <slot />
  </body>
</html>
